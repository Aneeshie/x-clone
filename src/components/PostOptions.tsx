import React from "react";
import ImageKit from "./Image";

const PostOptions = () => {
  return (
    <div className="cursor-pointer size-4 relative">
      <ImageKit path="icons/infoMore.svg" alt="" w={16} h={16} />
    </div>
  );
};

export default PostOptions;
